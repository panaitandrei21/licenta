import { RemoveUUIDPipe } from './remove-uuid.pipe';

describe('RemoveUUIDPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveUUIDPipe();
    expect(pipe).toBeTruthy();
  });
});
